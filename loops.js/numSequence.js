function numSequence(input) { 

    let text = input[0];

    for (let index = 0; index < text.length; index++) {
        
        console.log(text[index]);
        
    }
    
}
numSequence(["softuni"])